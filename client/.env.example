# Backend API base URL (no trailing slash, no /api suffix)
# Leave blank in development — Vite proxy handles it automatically
# Set to your deployed backend URL in production, e.g.:
#   VITE_API_URL=https://api.swiftpharma.com
VITE_API_URL=
